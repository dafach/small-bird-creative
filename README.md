# Small Bird Creative

Static marketing page. Vite and Three.js.

Scripts: dev, build, preview.

Install dependencies, then start the dev server. For production, run the build script. Files go to dist.

The publish directory is dist. Domain cutover waits for Adam.

Consult booking uses the Cal.com AI Systems Consultation page.
No secrets in this project.

Local: install packages, then use the dev script.
Production: use the build script; output is the dist folder.
